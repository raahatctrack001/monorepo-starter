/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: [
        'www.shutterstock.com', 
        "images.unsplash.com", 
        "randomuser.me", 
        "static.vecteezy.com"
      ],
    },
};

export default nextConfig;
